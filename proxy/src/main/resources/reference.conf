{
    server {
        host = "localhost"
        port = 8083
        request-timeout = 60s
    }

    grpc-endpoint {
        host = "localhost"
        port = 9999
    }

    circuit-breaker {
        retry-times = 3
        initial-retry-delay = 2s
    }
}